import svgText from '../../../utils/svg/text';
import { module, test } from 'qunit';

module('Unit | Utility | svg/text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = svgText();
  assert.ok(result);
});
