import { transition } from '../../../helpers/transition';
import { module, test } from 'qunit';

module('Unit | Helper | transition');

// Replace this with your real tests.
test('it works', function(assert) {
  //var result = transition(42);
  //assert.ok(result);
});
