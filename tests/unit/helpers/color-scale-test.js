import { colorScale } from '../../../helpers/color-scale';
import { module, test } from 'qunit';

module('Unit | Helper | color scale');

// Replace this with your real tests.
test('it works', function(assert) {
  //var result = colorScale(42);
  //assert.ok(result);
});
